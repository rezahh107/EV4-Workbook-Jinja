# استاندارد بخش تنظیمات، مقدارها و واحدها — v29

- هر درس دقیقاً یک disclosure بسته‌شونده دارد.
- ادعای واحدهای UI فقط با Help Center رسمی Elementor ثبت می‌شود.
- قابلیت CSS که در UI تأیید نشده با برچسب CSS/Custom CSS می‌آید.
- هر بخش باید مرجع محاسبه، کاربرد، تله، Responsive و DevTools را توضیح دهد.
- در درس‌های بدون واحد طول، `not_applicable` یا نوع مقدار keyword/reference/unitless صریح نوشته می‌شود.
- توصیه‌ها context-aware هستند؛ «همیشه rem» یا «هرگز px» ممنوع است.
