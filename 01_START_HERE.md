# TUYA Elementor V4 — Start Here

این بسته برای ادامهٔ بازسازی سکشن TUYA در یک گفت‌وگوی جدید آماده شده است.

## ترتیب استفاده

1. فایل `02_HANDOFF_PROMPT_FA.md` را کامل در پیام اول گفت‌وگوی جدید کپی کن.
2. هم‌زمان این فایل‌ها را به گفت‌وگوی جدید پیوست کن:
   - `TUYA_Standalone_Workbook_v32_0_0.html`
   - `tuya_ref_4.jpg`
   - `tuya-cloud-elementor-optimized.svg`
   - یک اسکرین‌شات تازه از وضعیت فعلی Elementor که کل Orbit Stage، Core، هر شش Node و پنجره Structure را نشان دهد.
3. اگر مدل به وضعیت قبلی نیاز داشت، `03_CURRENT_STATE.md` و `04_DECISION_AND_CONFLICT_LOG.md` را نیز در اختیارش بگذار.
4. اسکرین‌شات‌های پوشهٔ `evidence/screenshots` تاریخی‌اند و وضعیت نهایی بعد از ساخت شش Node را نشان نمی‌دهند؛ فقط برای فهم روند و رابط Elementor V4 استفاده شوند.

## اولین خروجی مورد انتظار از مدل جدید

مدل نباید فوراً دستور ساخت عنصر بعدی بدهد. ابتدا باید:

- فایل‌ها را بخواند؛
- وضعیت فعلی را با برچسب‌های `confirmed`، `provisional` و `unknown` بازگو کند؛
- از روی اسکرین‌شات تازه، ترکیب Desktop را بررسی کند؛
- فقط یک قدم کوچک بعدی پیشنهاد دهد؛
- منتظر تأیید بماند.
